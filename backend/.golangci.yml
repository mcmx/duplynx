version: "2"

run:
  timeout: 5m
  tests: true
  go: "1.24"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
  disable:
    - exhaustruct

  settings:
    staticcheck:

issues:
  max-same-issues: 3
  max-issues-per-linter: 50
